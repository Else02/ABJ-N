import styles from "../styles/results.module.css"

function results() {
    return (
        <div className={`${styles.nav}`}>
            <b>ABJ-N</b>
            <b className={`${styles.text}`}>Results</b>
        </div>
      
    );
  }

  export default results